spring:
  application:
    name: housework

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true

  cloud:
    aws:
      s3:
        enabled: false

file:
  upload:
    directory: ./uploads